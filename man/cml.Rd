% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cml.R
\name{cml}
\alias{cml}
\title{CML Algorithm}
\usage{
cml(
  data = NULL,
  true_dag = NULL,
  targets,
  node_names = NULL,
  lmax = 3,
  tol = 0.01,
  mb_tol = 0.05,
  method = "MMPC",
  test = "testIndFisher",
  verbose = TRUE
)
}
\arguments{
\item{data}{A data matrix or data frame, or \code{NULL} for the population
(oracle) version.}

\item{true_dag}{A 0/1 adjacency matrix of the true DAG, or \code{NULL} to
estimate the neighborhoods from \code{data} instead.}

\item{targets}{A vector of 1-based target node indices.}

\item{node_names}{Character vector of node names; defaults to \code{V0},
\code{V1}, ... if not supplied.}

\item{lmax}{Maximum size of the conditioning set considered during the
skeleton search.}

\item{tol}{Significance level for the conditional independence tests
used by the skeleton search.}

\item{mb_tol}{Significance level used for Markov Blanket estimation (see
\code{get_all_mbs()}); only relevant when \code{true_dag} is \code{NULL}.}

\item{method}{Markov Blanket estimation algorithm (see \code{get_mb()}); only
relevant when \code{true_dag} is \code{NULL}.}

\item{test}{The conditional independence test to use.}

\item{verbose}{Whether to provide detailed output.}
}
\value{
A list with the estimated adjacency matrix (\code{amat}), separating
sets (\code{S}), the number of conditional independence tests used
(\code{NumTests}, \code{MBNumTests}), the nodes considered (\code{Nodes}, in 1-based R
numbering), which FCI orientation rules were used (\code{RulesUsed}), timing
information, the reference DAG used (\code{referenceDAG}), the estimated
Markov Blanket list (\code{mbList}, if applicable), and, when \code{data} was
supplied, the pre-scaling column means/covariance (\code{data_means},
\code{data_cov}).
}
\description{
\code{cml()} applies the CML (Coordinated Multi-Neighborhood Learning)
algorithm to a dataset over certain target neighborhoods, which may be
provided by the user through the true DAG or may be estimated by a
Markov Blanket estimation algorithm.
}
\details{
Exactly one of \code{data} or \code{true_dag} (population/oracle version) must
ultimately be usable to identify each target's neighborhood: if
\code{true_dag} is \code{NULL}, the neighborhoods are first estimated from \code{data}
via \code{get_all_mbs()}; otherwise \code{true_dag} is used directly (with \code{data},
if also supplied, used only for the conditional independence tests
themselves -- the "semi-sample" version).
}

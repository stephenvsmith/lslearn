# Calculate Euclidean Distance between two vectors

Calculate Euclidean Distance between two vectors

## Usage

``` r
euclidean_dist(x, y)
```

## Arguments

- x:

  NumericVector

- y:

  NumericVector

## Value

double Euclidean distance
